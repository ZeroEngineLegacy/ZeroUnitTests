To install templates, zip the contents UnitTestTemplate folder and rename it from ".zip" to ".zerotemplate".
Drag the .zerotemplate file into the launcher to install it.

The BadTestProjects are tests that should fail.